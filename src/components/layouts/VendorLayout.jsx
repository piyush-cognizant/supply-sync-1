import React from 'react'
import { Outlet } from 'react-router'

const VendorLayout = () => {
    return (
        <div>
            VendorLayout
            <Outlet />
        </div>
    )
}

export default VendorLayout